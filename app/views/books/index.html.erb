<%- model_class = Book -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:"Book id") %></th>
      <th><%= model_class.human_attribute_name(:book_title) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.id %></td>
        <td><%= link_to book.book_title, book_path(book) %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_book_path(book), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      book_path(book),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Book', new_book_path, :class => 'btn btn-primary' %> 
<%= link_to 'New Author', new_author_path, :class => 'btn btn-default' %> 
<%= link_to 'New Topic', new_topic_path, :class => 'btn btn-primary' %>
<%= link_to "New Book's Authors", new_book_author_path, :class => 'btn btn-default' %>
<%= link_to "New Book's Topics", new_book_topic_path, :class => 'btn btn-primary' %>
<br><br>
<%= link_to "List Authors", authors_path, :class => 'btn btn-default' %>
<%= link_to "List Topics", topics_path, :class => 'btn btn-primary' %>
<%= link_to "List Book's Authors", book_authors_path, :class => 'btn btn-default' %>
<%= link_to "List Book's Topics", book_topics_path, :class => 'btn btn-primary' %>