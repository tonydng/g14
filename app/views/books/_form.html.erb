<%= form_for @book, :html => { :class => "form-horizontal book" } do |f| %>

  <% if @book.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @book.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :book_name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :book_name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:book_name) %>
  </div>
  <div class="form-group">
    <%= f.label :publisher_id, "Publisher ID", :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :publisher_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:publisher_id) %>
  </div>
  <div class="form-group">
    <%= f.label :topic_id, "Topic ID", :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :topic_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:topic_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                books_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
